/**
 * Conversation-related configuration constants
 */

/**
 * Maximum length for conversation titles (characters)
 */
export const MAX_CONVERSATION_TITLE_LENGTH = 100;

/**
 * Number of days to look back for recent conversation history
 */
export const RECENT_CONVERSATION_DAYS = 30;

/**
 * Maximum number of recent conversations to fetch
 */
export const MAX_RECENT_CONVERSATIONS = 10;

/**
 * Minimum number of messages required to generate a personality profile
 */
export const MIN_MESSAGES_FOR_PROFILE = 2;
